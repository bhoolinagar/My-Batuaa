package com.myBatuaa.controller;

public class UserController {

	/*
	 * JWT spring security(skip),  login , session mangment ,logout, signup,
"edit profile, picture update, pancard,  new bank add(multiple account) +TransactionHistory(recent Date)
+MoneyTransfer(dependent microservice on wallet)
+GetBalance(
+  boolean Logout
+getUserProfile(int userId)
+resetPassword(email, old password, new password)
+update profile( Image, address)
+add new bank details
"
	 * 
	 */
}
