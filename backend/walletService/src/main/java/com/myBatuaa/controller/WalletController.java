package com.myBatuaa.controller;

public class WalletController {
	/*
	 * "createAnAccount(String userId)
+ getAccountByUserId(String userId)
+ getTransactionHistory(String  userid)
+ Deposit( String accountId, accountNumber, BigDecimal Amount)
+Withdrawal(String accountNumber, BigDecimal Amount)
+void TransderMoney(String AccountNumberToAccountNumber, BigDecimal Amount)-- 
+Free bracket calculator-chargeable)"
	 */
	
}
